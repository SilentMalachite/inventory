# Inventory System configuration (sample)

# Where to store DB and logs (ensure writable)
# INVENTORY_APP_DIR=/var/lib/inventory

# Protect API endpoints with an API key (send in X-API-Key header)
# INVENTORY_API_KEY=change-me

# Basic auth for web UI forms (optional)
# INVENTORY_BASIC_USER=admin
# INVENTORY_BASIC_PASS=strong-password

# Disable audit logging (useful in CI)
# INVENTORY_AUDIT_DISABLED=1

# Log to stdout instead of file
# INVENTORY_AUDIT_STDOUT=1

# Run lightweight SQLite migrations on startup
# INVENTORY_MIGRATE=1

